spring:
  application:
    name: warehouse-data-ingestion-service-test
  main:
    allow-bean-definition-overriding: true
  servlet:
    multipart:
      enabled: true

# Keep logging minimal in tests
logging:
  level:
    root: WARN
    com.ikea.warehouse_data_ingestion_service: INFO
